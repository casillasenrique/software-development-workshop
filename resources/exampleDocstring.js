/**
 * One line title goes here. Keep it under 80 columns.
 *
 * Add optional additional descriptions here. This can be many lines long and
 * can include different sections.
 *
 * @param {string} text - Briefly describe the parameter.
 * @param {number} x - Briefly describe the parameter. It is ok if your
 *    description goes on another line.
 * @returns {boolean} Describe the returned value here
 */
function foo(text, x) {
  // Implementation here
  return;
}
